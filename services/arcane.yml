# Mot de passe initial pour Arcane: arcane / arcane-admin

#services:
#  arcane:
services:
  arcane:
    image: ghcr.io/getarcaneapp/arcane:latest
    container_name: arcane
    ports:
      - '3552:3552'
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - arcane-data:/app/data
      - /host/path/to/projects:/app/data/projects
    environment:
      - APP_URL=http://localhost:3552
      - PUID=1000
      - PGID=1000
      - ENCRYPTION_KEY=RBXVjvUbwqG4BQUwpNNDeLmhj0vo9V2T2bqfHSec2Gs=
      - JWT_SECRET=kIaZR20jO9x0rgbzXcYI6/uypvnsi8xh8fy2TVGyC/0=¸
    restart: unless-stopped

volumes:
  arcane-data:

