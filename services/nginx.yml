services:
  nginx:
    image: nginx:latest
    container_name: nginx
    restart: unless-stopped
    ports:
      - "${NGINX_PORT}:80"

    volumes:
      - nginx_html:/usr/share/nginx/html
    networks:
      - reseauWP

volumes:

  nginx_html: